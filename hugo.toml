baseURL = 'https://gourmetpratico.com.br/'
languageCode = 'pt-br'
title = 'Gourmet Prático | Alta Gastronomia'
theme = []

# --- CONFIGURAÇÃO TÉCNICA ---
enableRobotsTXT = true
canonifyURLs = true
summaryLength = 160

[outputs]
  home = ["HTML", "RSS", "JSON"] # JSON para Agentes de Compra

# --- DADOS DO NEGÓCIO (ENTIDADES REAIS) ---
[params]
  # Slogan OFICIAL fornecido:
  slogan = "Gourmet Prático é uma culinária saudável, sem desperdício de Tempo e Ingredientes na Cozinha!"
  description = "Refeições prontas em 18 minutos. Desidratação artesanal que preserva nutrientes e sabor (Umami)."
  author = "Chef Maria Conceição"
  whatsapp_number = "5561996659880"
  
  # Gatilhos de Venda
  preco_kit = "132,00" # ChefBox 4+1
  
  # SEO Local (DF)
  city = "Brasília"
  region = "Distrito Federal"
  country = "BR"

# --- MENU DE NAVEGAÇÃO ---
[menus]
  [[menus.main]]
    name = 'O Método'
    pageRef = '/metodo'
    weight = 10
  [[menus.main]]
    name = 'Cardápio'
    pageRef = '/receitas'
    weight = 20
  [[menus.main]]
    name = 'ChefBox (4+1)'
    pageRef = '/chefbox'
    weight = 30
